# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# macOS
.DS_Store

# Fichiers temporaires Office (lock files)
~$*

# Config Claude Code locale (spécifique à la machine)
.claude/settings.local.json

# EDA (à ajouter plus tard)
notebooks/03_eda.ipynb
reports/figures/

# Données brutes (téléchargées manuellement par chacun, volumineuses / parfois
# sous licence -> jamais versionnées). Seules les tables construites par les
# pipelines (data/processed/dim_*.parquet, petites, dérivées) sont commit.
data/raw/
